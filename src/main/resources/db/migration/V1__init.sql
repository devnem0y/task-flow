
-- Расширение для генерации UUID (понадобится позже)
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Начальная схема будет добавляться в следующих уроках
-- Эта миграция только проверяет, что Flyway подключён и работает